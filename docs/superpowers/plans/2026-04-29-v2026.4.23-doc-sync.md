# Hermes Agent Docs Sync — v2026.4.23 (v0.11.0)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:dispatching-parallel-agents and superpowers:executing-plans to execute this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bring `~/src/hermes/hermes-agent-docs` to factual parity with the latest *released* state of `~/src/hermes/hermes-agent` at tag `v2026.4.23` / Hermes Agent **v0.11.0**, then commit and push.

**Approach:** Treat upstream `RELEASE_v0.11.0.md`, `website/docs/**` (at tag `v2026.4.23`), and the source code at that tag as the only normative inputs. Disregard unreleased work on `main` past `v2026.4.23`. Use parallel subagents per subsystem to do the validation, surface deltas, and propose edits. Apply edits centrally, then produce a 4-file audit artifact set bound to `v2026.4.23` mirroring the existing v2026.4.16 set.

**Tech context:** Pure docs work — Markdown only. No source-tree edits. Validate claims against Python source under `agent/`, `gateway/`, `cli/`, `tools/`, `plugins/`, `tui_gateway/`, `ui-tui/`, and `website/docs/**` at `v2026.4.23`.

---

## Release boundary

- Latest *released* tag in `~/src/hermes/hermes-agent`: `v2026.4.23`
- Marketed version: **Hermes Agent v0.11.0** (April 23, 2026)
- Last audit set in docs repo: `v2026.4.16` (Hermes Agent v0.10.0)
- Diff window for this pass: `v2026.4.16..v2026.4.23` (1,219 commits, 1,137 files, +187,779 / −21,599)
- Authoritative release artifact in source: `RELEASE_v0.11.0.md`
- Authoritative source docs tree: `website/docs/**` at `v2026.4.23`
- Online cross-checks (secondary): GitHub release page, GitHub compare URL, README at tag

Any unreleased commits on `main` past `v2026.4.23` are out of scope. Where public-release-surface metadata disagrees with local artifacts, record it in the audit notes rather than silently normalizing.

---

## Already correct (verified before planning)

- `README.md` — declares `v0.11.0 (v2026.4.23)`, lists v0.11.0 highlights and providers.
- `changelog.md` — has `## v0.11.0 -- April 23, 2026` section with delivery notes and full-changelog link.

These two files still need a **claim-level re-check** against `RELEASE_v0.11.0.md` (Task 6 / Task 13), but no high-level rewrite is needed.

---

## Known doc-repo gaps before this pass

1. **Missing audit artifact set for v2026.4.23.** Existing audit artifacts stop at `v2026.4.16`:
   - `review_plan_v2026.4.16.md`
   - `release_evaluation_v2026.4.16.md`
   - `repo_review_v2026.4.16.md`
   - `source_validation_matrix_v2026.4.16.md`
   - `documentation_integrity_findings_v2026.4.16.md`
2. **Missing pages** (present upstream at `v2026.4.23`, absent in docs repo):
   - `tui.md` (new in v0.11.0 — Ink-based TUI user guide)
   - `built-in-plugins.md` (new in v0.11.0 — bundled plugin reference, e.g. disk-cleanup)
   - `guides/github-pr-review-agent.md` (new guide)
   - `guides/webhook-github-pr-review.md` (new guide)
3. **Pages whose claims need v0.11.0 re-validation** (modified upstream in window):
   - User guide: `cli.md`, `configuration.md`, `docker.md`, `profiles.md`, `sessions.md`
   - Features: `api-server.md`, `browser.md`, `code-execution.md`, `cron.md`, `delegation.md`, `fallback-providers.md`, `honcho.md`, `hooks.md`, `image-generation.md`, `memory-providers.md`, `overview.md`, `plugins.md`, `rl-training.md`, `skills.md`, `tool-gateway.md`, `tts.md`, `vision.md`, `voice-mode.md`, `web-dashboard.md`
   - Messaging: `dingtalk.md`, `discord.md`, `feishu.md`, `index.md`, `matrix.md`, `open-webui.md`, `qqbot.md`, `slack.md`, `telegram.md`, `webhooks.md` (mapped to docs repo `messaging/webhooks.md`), `wecom.md`, `weixin.md`
   - Developer guide: `adding-providers.md`, `agent-loop.md`, `architecture.md`, `contributing.md`, `creating-skills.md`, `gateway-internals.md`
   - Reference: `cli-commands.md`, `environment-variables.md`, `faq.md`, `optional-skills-catalog.md`, `profile-commands.md`, `skills-catalog.md`, `slash-commands.md`, `tools-reference.md`, `toolsets-reference.md`
   - Getting started: `installation.md`, `learning-path.md`, `quickstart.md`, `updating.md`, `nix-setup.md`, `termux.md`
   - Guides: `build-a-hermes-plugin.md`, `daily-briefing-bot.md`, `delegation-patterns.md`, `use-voice-mode-with-hermes.md`
   - Integrations: `providers.md`
4. **Probable claim drift** to watch for (from `RELEASE_v0.11.0.md`):
   - **Transport ABC** under `agent/transports/` — `AnthropicTransport`, `ChatCompletionsTransport`, `ResponsesApiTransport`, `BedrockTransport` (architecture / developer-guide / providers).
   - **TUI** — `hermes --tui`, `HERMES_TUI=1`, `tui_gateway` JSON-RPC backend, OSC-52 clipboard, status bar with stopwatch & git branch, `/clear` confirm, light-theme preset, subagent observability overlay.
   - **New providers** in v0.11.0: NVIDIA NIM, Arcee AI, Step Plan, Google Gemini CLI OAuth (`HERMES_GEMINI_PROJECT_ID`), Vercel ai-gateway native path, GPT-5.5 over Codex OAuth, Bedrock native (Converse + IAM + inference profiles + Guardrails).
   - **Plugin surface expansion**: `register_command`, `dispatch_tool`, `pre_tool_call` veto, `transform_tool_result`, `transform_terminal_output`, image_gen plugin backends, namespaced skill bundles, shell-script hooks, dashboard tabs.
   - **`/steer`** mid-run nudge command.
   - **Webhook direct-delivery mode** — zero-LLM push payloads.
   - **Delegation orchestrator role** with `delegation.max_spawn_depth` (1, range 1–3) and `delegation.orchestrator_enabled`.
   - **QQBot** as 17th platform (via QQ Official API v2 with QR setup wizard, emoji reactions, DM/group policy gating).
   - **Auxiliary models UI** screen for compression / vision / session_search / title_generation overrides.
   - **Cron**: `wakeAgent` gate (script-only jobs), per-job `enabled_toolsets`.
   - **Skills changes**: new skills (`concept-diagrams`, `architecture-diagram`, `pixel-art`, `baoyu-comic`, `baoyu-infographic`, `page-agent`, `drug-discovery`, `touchdesigner-mcp`, `adversarial-ux-test`); `xitter` → `xurl`; `hermes skills reset`.
   - **CLI flags**: `--ignore-user-config`, `--ignore-rules`. Doctor "Command Installation" check. Dynamic shell completion (bash/zsh/fish).
   - **Live-switching dashboard themes**, dashboard plugin tabs.

---

## File structure

This plan only edits files under `~/src/hermes/hermes-agent-docs/`. New files to create:

| Path | Responsibility |
|------|---------------|
| `tui.md` | User guide for the v0.11.0 Ink-based TUI (`hermes --tui`, `HERMES_TUI=1`, JSON-RPC backend, sticky composer, status bar, observability overlay). |
| `built-in-plugins.md` | Reference for bundled plugins shipped in v0.11.0 (e.g. `disk-cleanup` opt-in default). |
| `guides/github-pr-review-agent.md` | Guide imported from upstream `website/docs/guides/github-pr-review-agent.md`. |
| `guides/webhook-github-pr-review.md` | Guide imported from upstream `website/docs/guides/webhook-github-pr-review.md`. |
| `review_plan_v2026.4.23.md` | Release-bounded review plan for the v0.11.0 audit. |
| `release_evaluation_v2026.4.23.md` | Release validation, diff sizing, public-release surface notes. |
| `repo_review_v2026.4.23.md` | Source-tree review mapped by subsystem, with v0.11.0 deltas. |
| `source_validation_matrix_v2026.4.23.md` | Claim-to-source matrix for the v2026.4.16..v2026.4.23 sync window. |
| `documentation_integrity_findings_v2026.4.23.md` | Findings + remediations recorded during this pass. |

Files modified are listed inline per task. No source-tree files outside the docs repo are touched.

---

## Subagent fan-out (Tasks 1–9 in parallel)

These nine tasks are fully independent — they read code at tag `v2026.4.23` and report findings. Each task spawns a **single Explore subagent** with a self-contained brief. No subagent edits docs; they only return a structured report. Reports are aggregated by the orchestrator (this plan's executor) and turned into edits in Tasks 10–14.

The orchestrator dispatches Tasks 1–9 in **one tool message** (multiple `Agent` tool calls) for parallelism.

### Task 1: Audit Core Agent + Transport ABC

**Files (read-only inputs at tag v2026.4.23):**
- `agent/transports/*.py`, `agent/run_agent.py`, `agent/auxiliary_client.py`, `agent/compressor*.py`, `agent/cli/normalize_anthropic_response*.py`
- `RELEASE_v0.11.0.md` (sections "Transport Layer", "Provider & Model Support", "Agent Loop & Conversation", "Session & Memory")
- Docs targets: `architecture.md`, `developer-guide/architecture.md`, `developer-guide/agent-loop.md`, `developer-guide/transport-layer.md`, `developer-guide/context-compression.md`, `developer-guide/context-compression-and-caching.md`

- [ ] **Step 1: Brief the subagent**

Spawn an Explore subagent with this prompt:
```
Working dir: ~/src/hermes/hermes-agent. Check out tag v2026.4.23 in read-only mode (do not modify the tree — use `git show v2026.4.23:<path>` or `git -c advice.detachedHead=false worktree`-equivalent reads). Validate the following claims against the source at v2026.4.23:

1. Transport ABC lives at agent/transports/ with concrete implementations AnthropicTransport, ChatCompletionsTransport, ResponsesApiTransport, BedrockTransport. Confirm the abstract base class name, file paths, and the methods each transport implements (message conversion, tool conversion, kwargs assembly, response normalization).
2. Streaming, retries, prompt cache, and credential refresh remain on AIAgent (not on transports).
3. /steer command implementation: where is the handler? What does it inject? Does it preserve prompt cache?
4. Compressor: protect_first_n default 3, protect_last_n default 20, threshold default 50%, language-aware summaries, fallback to main model on permanent 503/404.
5. Auto-prune sessions + VACUUM state.db on startup.

Report findings as a markdown table: Claim | Source evidence (file:line) | Verdict (CONFIRMED / DRIFTED / MISSING) | Suggested doc fix. Cap report at 400 lines. Do not edit any files.
```

- [ ] **Step 2: Aggregate report into orchestrator notes**

Save the returned report verbatim under `.audit/task01-core-agent.md` (gitignored scratch — see Step 0 of Task 10). Do not commit.

- [ ] **Step 3: Mark Task 1 complete in TaskList**

### Task 2: Audit Provider Layer (new providers + existing)

**Files:**
- `agent/providers/*.py`, `agent/cli/model.py`, `agent/cli/setup_*.py`, `agent/auth/*.py`
- `RELEASE_v0.11.0.md` § "Provider & Model Support", "Provider Setup & UX"
- Docs targets: `providers.md`, `integrations/providers.md`, `developer-guide/adding-providers.md`, `developer-guide/provider-runtime.md`, `provider-routing.md`, `nous-tool-gateway.md`, `tool-gateway.md`, `guides/aws-bedrock.md`, `README.md` (provider list)

Validate claims:
1. Each new v0.11.0 provider exists as a module: NVIDIA NIM, Arcee AI, Step Plan, Google Gemini CLI OAuth, Vercel ai-gateway, Codex OAuth (GPT-5.5), GMI Cloud (OpenAI-compatible).
2. Bedrock: Converse API, IAM auth, inference profiles (`us.` / `global.`), Guardrails support, region-aware discovery.
3. Gemini routed through native AI Studio API path.
4. xAI Grok upgraded to Responses API.
5. Per-provider + per-model `request_timeout_seconds`. `agent.api_max_retries` config.
6. Auxiliary models UI screen and main-model-first auto routing.
7. Confirm provider list in README.md against actual provider modules at v2026.4.23.

Same report shape as Task 1; save to `.audit/task02-providers.md`.

- [ ] Step 1: Brief subagent  
- [ ] Step 2: Aggregate report  
- [ ] Step 3: Mark complete

### Task 3: Audit Plugin / Skill / Hook Surface

**Files:**
- `plugins/*.py`, `tools/plugin_loader*.py`, `tools/skill_*.py`, `tools/hooks/*`, bundled plugin source under `plugins/builtin/` (disk-cleanup etc.)
- `RELEASE_v0.11.0.md` § "Plugin & Extensibility", "Skills"
- Docs targets: `plugins.md`, `dashboard-plugins.md`, `hooks.md`, `skills.md`, `built-in-plugins.md` (to be created), `guides/build-a-hermes-plugin.md`, `developer-guide/creating-skills.md`, `reference/skills-catalog.md`, `reference/optional-skills-catalog.md`

Validate claims:
1. Plugin APIs exposed: `register_command`, `register_cli_command`, `dispatch_tool`, `transform_tool_result`, `transform_terminal_output`, `image_gen` backend registration, dashboard tabs, namespaced skill bundles, shell-script hook bridge, `pre_tool_call` veto.
2. Lifecycle hooks fire in CLI + gateway: `pre_llm_call`, `post_llm_call`, `on_session_start`, `on_session_end`, `pre_tool_call`, `post_tool_call`.
3. Bundled plugin list — confirm what is bundled vs opt-in by default (disk-cleanup opt-in is the named example).
4. New skills shipped in v0.11.0: `concept-diagrams`, `architecture-diagram`, `pixel-art`, `baoyu-comic`, `baoyu-infographic`, `page-agent`, `drug-discovery`, `touchdesigner-mcp`, `adversarial-ux-test`.
5. `xitter` → `xurl` rename.
6. `hermes skills reset` command exists.
7. Skills count claim in README ("118 skills (96 bundled + 22 optional)") — confirm against actual skill manifests at v2026.4.23.

Same report shape; save to `.audit/task03-plugins-skills.md`.

- [ ] Step 1: Brief subagent  
- [ ] Step 2: Aggregate report  
- [ ] Step 3: Mark complete

### Task 4: Audit CLI / TUI / Slash Commands

**Files:**
- `cli/*.py`, `hermes_cli/*.py`, `ui-tui/**`, `tui_gateway/**`, slash command registry, `cli/setup_*.py`
- `RELEASE_v0.11.0.md` § "TUI", "CLI & UX"
- Docs targets: `cli-reference.md`, `reference/cli-commands.md`, `reference/slash-commands.md`, `reference/profile-commands.md`, `tui.md` (to be created), `live-model-switching.md`, `notify-on-complete.md`, `skins.md`, `getting-started/quickstart.md`, `getting-started/installation.md`, `getting-started/updating.md`, `getting-started/learning-path.md`, `getting-started/termux.md`

Validate claims:
1. `hermes --tui` flag and `HERMES_TUI=1` env var route to the Ink-based TUI; `tui_gateway` is the Python JSON-RPC backend; both are present in the source tree.
2. New slash commands in v0.11.0: `/steer`. Confirm `/clear`, `/queue`, `/permission`, `/browser`, `/personality`, `/retry`, `/undo`, `/compress`, `/usage`, `/insights`, `/cost`, `/approve`, `/deny`, `/verbose`, `/skills`, `/stop`, `/platforms`, `/status`, `/sethome` still exist and behavior matches docs.
3. New CLI flags: `--ignore-user-config`, `--ignore-rules`. New `hermes skills reset` subcommand. New Doctor "Command Installation" check. Dynamic completion install paths (bash/zsh/fish).
4. Status bar features: per-turn stopwatch, git branch, OSC-52 clipboard, light-theme preset, `/clear` confirm.
5. Subagent spawn observability overlay exists in TUI.

Same report shape; save to `.audit/task04-cli-tui.md`.

- [ ] Step 1: Brief subagent  
- [ ] Step 2: Aggregate report  
- [ ] Step 3: Mark complete

### Task 5: Audit Gateway / Messaging Adapters

**Files:**
- `gateway/**`, `gateway/adapters/*.py`, `gateway/webhook/*.py`, adapter setup wizards
- `RELEASE_v0.11.0.md` § "Messaging", "Webhook direct-delivery", "Gateway"
- Docs targets: `gateway.md`, `messaging/index.md`, `messaging/qqbot.md`, `messaging/webhooks.md`, `messaging/{telegram,discord,slack,whatsapp,signal,email,matrix,mattermost,dingtalk,homeassistant,feishu,wecom,weixin,bluebubbles,sms,open-webui}.md`, `developer-guide/adding-platform-adapters.md`, `developer-guide/gateway-internals.md`

Validate claims:
1. 17 platforms total. List of platforms in README matches actual adapters at v2026.4.23.
2. QQBot: QQ Official API v2, QR setup wizard, streaming cursor, emoji reactions, DM/group policy gating.
3. Webhook direct-delivery mode — payloads forwarded to platform chat without LLM. Locate the config option.
4. Six new adapters added in v0.4.0 — sanity check.
5. Auto-reconnect + exponential backoff (claim from v0.4.0; still true at v0.11.0).
6. PLATFORM_HINTS for Matrix, Mattermost, Feishu.
7. Streaming gateway support: Telegram, Discord, Slack, CLI; WhatsApp/Signal/Email/Home Assistant fall back to non-streaming.

Same report shape; save to `.audit/task05-gateway.md`.

- [ ] Step 1: Brief subagent  
- [ ] Step 2: Aggregate report  
- [ ] Step 3: Mark complete

### Task 6: Audit Cron / Delegation / Curator

**Files:**
- `cron/*.py`, `agent/cron_*.py`, `agent/delegation*.py`, `tools/delegate_task*.py`, `agent/curator/*.py`
- `RELEASE_v0.11.0.md` § "Cron", "Delegation"
- Docs targets: `cron.md`, `delegation.md`, `developer-guide/cron-internals.md`, `guides/automate-with-cron.md`, `guides/automation-templates.md`, `guides/cron-troubleshooting.md`, `guides/delegation-patterns.md`

Validate claims:
1. Cron `wakeAgent` gate — confirm config flag and that scripts can run without invoking LLM.
2. Cron per-job `enabled_toolsets`.
3. Delegation `orchestrator` role + `delegation.max_spawn_depth` (default 1, range 1-3) + `delegation.orchestrator_enabled` toggle.
4. Concurrent sibling subagents share filesystem state via a file-coordination layer.
5. Curator (background skill maintenance): only on `main`? Or also at v2026.4.23? If post-tag, exclude from this audit.

Same report shape; save to `.audit/task06-cron-delegation.md`.

- [ ] Step 1: Brief subagent  
- [ ] Step 2: Aggregate report  
- [ ] Step 3: Mark complete

### Task 7: Audit Tools / Security / Memory

**Files:**
- `tools/*.py`, `agent/security/*.py`, `agent/redact.py`, `agent/memory*.py`, memory provider plugins source
- `RELEASE_v0.11.0.md` § "Tools", "Security & Privacy", "Memory"
- Docs targets: `tools.md` (if present), `reference/tools-reference.md`, `reference/toolsets-reference.md`, `code-execution.md`, `browser.md`, `mcp.md`, `image-generation.md`, `vision.md`, `tts.md`, `voice-mode.md`, `security.md` (if present), `memory.md`, `memory-providers.md`, `honcho.md`, `developer-guide/tools-runtime.md`, `developer-guide/memory-provider-plugin.md`, `credential-pools.md`, `context-references.md`

Validate claims:
1. Tool registry & dispatch — confirm tool list and any v0.11.0 changes.
2. Browser: Browserbase, Browser Use, CDP connect, Camofox via `CAMOFOX_URL`.
3. Image generation backends + plugin-provided backends; xAI image gen (`grok-imagine-image`).
4. MCP: stdio + HTTP transports, sampling, OAuth 2.1 PKCE, `hermes mcp serve` (stdio only at v0.11.0?).
5. Memory: built-in `MEMORY.md`, pluggable memory providers (Honcho), `on_memory_write` bridge.
6. Honcho 5-tool surface, session isolation, cost safety.
7. Credential pools `least_used` strategy + 401 refresh-and-rotate.
8. Secret redaction patterns.
9. PII redaction `privacy.redact_pii`.

Same report shape; save to `.audit/task07-tools-security-memory.md`.

- [ ] Step 1: Brief subagent  
- [ ] Step 2: Aggregate report  
- [ ] Step 3: Mark complete

### Task 8: Audit API Server / Dashboard / Voice

**Files:**
- `api/*.py`, `web/**` (frontend), `agent/voice*.py`
- `RELEASE_v0.11.0.md` § "API Server", "Dashboard", "Voice"
- Docs targets: `api-server.md`, `web-dashboard.md`, `dashboard-plugins.md`, `voice-mode.md`, `tts.md`, `messaging/open-webui.md`

Validate claims:
1. API server `/v1/chat/completions`, `/v1/responses`, `/api/jobs`, `Idempotency-Key`, `X-Hermes-Session-Id`, real-time tool progress streaming, `hermes-api-server` toolset.
2. Dashboard plugin tabs, theme system (six built-ins: Hermes Teal, Midnight, Ember, Mono, Cyberpunk, Rosé), `~/.hermes/dashboard-themes/`, `dashboard.theme` config key.
3. i18n English + Chinese, react-router sidebar, mobile-responsive, Vercel deployment, per-session API call tracking, one-click update + gateway restart buttons.
4. Voice: faster-whisper, Push-to-talk CLI, voice notes, Discord voice channels, `stt.enabled` flag.

Same report shape; save to `.audit/task08-api-dashboard-voice.md`.

- [ ] Step 1: Brief subagent  
- [ ] Step 2: Aggregate report  
- [ ] Step 3: Mark complete

### Task 9: Audit Configuration / Env Vars / Reference

**Files:**
- `agent/config*.py`, `cli/config_*.py`, env var usages
- `RELEASE_v0.11.0.md` § "Config & Env"
- Docs targets: `configuration.md`, `reference/environment-variables.md`, `reference/faq.md`, `reference/profile-commands.md`, `profiles.md`

Validate claims:
1. `cfg_get()` helper exists (added in v2026.4.23 window — confirm it was in the released tag and not post-tag).
2. `HERMES_HOME`, `HERMES_TUI`, `CAMOFOX_URL`, `HERMES_GEMINI_PROJECT_ID`, `HERMES_REDACT_SECRETS` documented.
3. Config keys mentioned in v0.11.0: `delegation.max_spawn_depth`, `delegation.orchestrator_enabled`, `dashboard.theme`, `agent.api_max_retries`, `skills.guard_agent_created`, `stt.enabled`, `privacy.redact_pii`, `security.redact_secrets`, `mcp_servers`.
4. Profiles: `hermes profile create`, `hermes -p <name> ...`, `hermes profile use <name>`, token locks across profiles.

Same report shape; save to `.audit/task09-config-env.md`.

- [ ] Step 1: Brief subagent  
- [ ] Step 2: Aggregate report  
- [ ] Step 3: Mark complete

---

## Edit phase (Tasks 10–14, sequential)

These tasks consume the nine `.audit/task*.md` reports and produce the actual doc edits.

### Task 10: Set up audit workspace

**Files:**
- Create: `~/src/hermes/hermes-agent-docs/.audit/` (gitignored — verify `.gitignore`)
- Modify: `~/src/hermes/hermes-agent-docs/.gitignore` (add `.audit/`)

- [ ] **Step 1: Add `.audit/` to `.gitignore`**

```bash
cd ~/src/hermes/hermes-agent-docs && \
  grep -qxF '.audit/' .gitignore 2>/dev/null || echo '.audit/' >> .gitignore
```

- [ ] **Step 2: Verify**

```bash
git -C ~/src/hermes/hermes-agent-docs check-ignore .audit/ ; echo $?
```
Expected: exit code 0 (path is ignored).

- [ ] **Step 3: Commit the .gitignore change**

```bash
cd ~/src/hermes/hermes-agent-docs && \
  git add .gitignore && \
  git commit -m "chore: ignore local .audit/ scratch directory used during release sync"
```

### Task 11: Create the four new content pages

**Files:**
- Create: `tui.md`, `built-in-plugins.md`, `guides/github-pr-review-agent.md`, `guides/webhook-github-pr-review.md`

**Source for each page:** the upstream file at `git show v2026.4.23:website/docs/<path>` from `~/src/hermes/hermes-agent`. The docs-repo style differs slightly (no Docusaurus frontmatter); strip frontmatter and adapt internal links to the docs-repo structure (e.g. `../user-guide/features/X` → `X.md`). Cross-reference with subagent reports from Tasks 3, 4, 5 for any v0.11.0-only behavior to add.

- [ ] **Step 1: Author `tui.md`**

Read upstream:
```bash
git -C ~/src/hermes/hermes-agent show v2026.4.23:website/docs/user-guide/tui.md > /tmp/tui-upstream.md
```
Adapt and write `~/src/hermes/hermes-agent-docs/tui.md`. Required sections (per upstream content + Task 4 findings):
- Activation (`hermes --tui`, `HERMES_TUI=1`)
- Architecture (Ink frontend + Python `tui_gateway` JSON-RPC)
- Composer & input behavior (sticky composer, multiline)
- Streaming + OSC-52 clipboard
- Status bar (stopwatch, git branch, model, profile)
- Themes (light-theme preset)
- `/clear` confirm
- Subagent spawn observability overlay
- Differences vs the prompt_toolkit CLI
- Troubleshooting (terminal capability detection, color depth)

- [ ] **Step 2: Author `built-in-plugins.md`**

Read upstream:
```bash
git -C ~/src/hermes/hermes-agent show v2026.4.23:website/docs/user-guide/features/built-in-plugins.md > /tmp/built-in-plugins-upstream.md
```
Adapt. Required sections (per upstream content + Task 3 findings):
- What ships bundled vs opt-in
- `disk-cleanup` reference plugin (opt-in default)
- How to enable/disable bundled plugins via `config.yaml`
- Pointer to plugin development guide

- [ ] **Step 3: Author `guides/github-pr-review-agent.md` and `guides/webhook-github-pr-review.md`**

Same procedure: pull upstream content from `v2026.4.23:website/docs/guides/...md`, adapt, write to `guides/` in the docs repo.

- [ ] **Step 4: Run a markdown sanity pass**

```bash
cd ~/src/hermes/hermes-agent-docs && \
  for f in tui.md built-in-plugins.md guides/github-pr-review-agent.md guides/webhook-github-pr-review.md; do \
    [ -f "$f" ] && echo "OK $f" || echo "MISSING $f"; \
  done
```
Expected: four `OK` lines.

- [ ] **Step 5: Commit**

```bash
cd ~/src/hermes/hermes-agent-docs && \
  git add tui.md built-in-plugins.md guides/github-pr-review-agent.md guides/webhook-github-pr-review.md && \
  git commit -m "docs(v0.11.0): add tui, built-in plugins, and PR review guides"
```

### Task 12: Patch existing pages with v0.11.0 deltas

For each of the docs listed in "Pages whose claims need v0.11.0 re-validation" (above), apply the corrections surfaced by the subagent reports. Each edit must trace to a specific `task<NN>` finding.

**Strategy:** group edits into commits by docs-repo subtree to keep history navigable.

- [ ] **Step 1: Patch features pages (commit 1)**

Cycle through `api-server.md`, `browser.md`, `code-execution.md`, `cron.md`, `delegation.md`, `fallback-providers.md`, `honcho.md`, `hooks.md`, `image-generation.md`, `memory-providers.md`, `overview.md`, `plugins.md`, `rl-training.md`, `skills.md`, `tool-gateway.md`, `tts.md`, `vision.md`, `voice-mode.md`, `web-dashboard.md`, `dashboard-plugins.md`, `live-model-switching.md`, `notify-on-complete.md`, `skins.md`. Apply only diffs that the audit reports flag as DRIFTED or MISSING.

```bash
cd ~/src/hermes/hermes-agent-docs && \
  git add <files> && \
  git commit -m "docs(v0.11.0): refresh feature pages against released v0.11.0 surface"
```

- [ ] **Step 2: Patch user-guide + getting-started pages (commit 2)**

Cycle through `cli-reference.md`, `configuration.md`, `docker.md`, `profiles.md`, `getting-started/{installation,quickstart,learning-path,nix-setup,termux,updating}.md`. Same approach.

```bash
git commit -m "docs(v0.11.0): refresh user guide and getting-started flows for v0.11.0"
```

- [ ] **Step 3: Patch messaging adapters (commit 3)**

Cycle through `messaging/{index,telegram,discord,slack,qqbot,webhooks,wecom,weixin,feishu,matrix,mattermost,dingtalk,open-webui}.md`. Confirm `webhooks.md` includes the new direct-delivery mode.

```bash
git commit -m "docs(messaging,v0.11.0): document QQBot v2 setup, direct webhook delivery, and PLATFORM_HINTS"
```

- [ ] **Step 4: Patch reference + developer-guide (commit 4)**

Cycle through `reference/{cli-commands,environment-variables,faq,optional-skills-catalog,profile-commands,skills-catalog,slash-commands,tools-reference,toolsets-reference}.md` and `developer-guide/{adding-providers,agent-loop,architecture,contributing,creating-skills,gateway-internals,transport-layer}.md`. The transport-layer page must reflect the v0.11.0 ABC.

```bash
git commit -m "docs(reference,dev-guide,v0.11.0): document /steer, transport ABC, new providers, new slash commands"
```

- [ ] **Step 5: Patch top-level pages (commit 5)**

`README.md`, `changelog.md`, `architecture.md`, `providers.md`, `integrations/providers.md`. The README and changelog already mention v0.11.0; this commit only fixes any drift the audits found (e.g. missing or incorrect fact).

```bash
git commit -m "docs(v0.11.0): claim-level fixes in README, changelog, and provider catalog"
```

### Task 13: Author the audit artifact set for v2026.4.23

**Files:**
- Create: `review_plan_v2026.4.23.md`, `release_evaluation_v2026.4.23.md`, `repo_review_v2026.4.23.md`, `source_validation_matrix_v2026.4.23.md`, `documentation_integrity_findings_v2026.4.23.md`

Mirror the structure of the existing `*_v2026.4.16.md` files. Each draws content from the nine subagent reports and from the actual edits applied in Task 12.

- [ ] **Step 1: `review_plan_v2026.4.23.md`** — same format as `review_plan_v2026.4.16.md`. Scope: `v2026.4.16..v2026.4.23` window. Workstreams = the nine subagent tasks.

- [ ] **Step 2: `source_validation_matrix_v2026.4.23.md`** — table mapping each upstream `website/docs/**` change to its docs-repo target with status.

- [ ] **Step 3: `release_evaluation_v2026.4.23.md`** — counts (1,219 commits, 1,137 files, +187,779/−21,599), key released features, public-release-surface notes (GitHub Releases ↔ local tag agreement).

- [ ] **Step 4: `repo_review_v2026.4.23.md`** — subsystem-by-subsystem digest with v0.11.0 deltas.

- [ ] **Step 5: `documentation_integrity_findings_v2026.4.23.md`** — any DRIFTED or MISSING findings that were not fixed (out-of-scope or upstream issues), with rationale.

- [ ] **Step 6: Commit the audit artifact set**

```bash
cd ~/src/hermes/hermes-agent-docs && \
  git add review_plan_v2026.4.23.md source_validation_matrix_v2026.4.23.md \
          release_evaluation_v2026.4.23.md repo_review_v2026.4.23.md \
          documentation_integrity_findings_v2026.4.23.md && \
  git commit -m "audit(v2026.4.23): release-bound docs sync artifacts for v0.11.0"
```

### Task 14: Online cross-check + final integrity sweep

Use WebFetch to compare the local `RELEASE_v0.11.0.md` against the public release page. Record any discrepancy in `documentation_integrity_findings_v2026.4.23.md` rather than silently normalizing.

- [ ] **Step 1: Fetch GitHub release page**

```text
WebFetch url=https://github.com/NousResearch/hermes-agent/releases/tag/v2026.4.23
       prompt="List the release name, date, and the main highlight bullets verbatim. Return as a markdown bullet list. If the page does not exist, say 'NO PUBLIC RELEASE'."
```

- [ ] **Step 2: Compare against `RELEASE_v0.11.0.md`** highlights table.

- [ ] **Step 3: If a discrepancy exists**, append a "Public release surface" section to `documentation_integrity_findings_v2026.4.23.md` and amend the commit from Task 13 (or add a new commit).

- [ ] **Step 4: Run a final repo-wide grep for stale version markers**

```bash
cd ~/src/hermes/hermes-agent-docs && \
  grep -RIn --include='*.md' -E 'v0\.10\.0|v2026\.4\.16' . | grep -vE 'changelog\.md|.*_v2026\.4\.(8|13|16)\.md|.audit/' | head -40
```
Expected: no hits beyond legitimate historical references inside changelog/audit-artifact files.

- [ ] **Step 5: Mark task complete in TaskList**

---

## Task 15: Commit and push

- [ ] **Step 1: Inspect the working tree**

```bash
cd ~/src/hermes/hermes-agent-docs && git status && git log --oneline origin/main..HEAD
```
Expected: a clean working tree (all edits already committed across Tasks 10–13) and a stack of new commits.

- [ ] **Step 2: Push**

```bash
cd ~/src/hermes/hermes-agent-docs && git push origin main
```

- [ ] **Step 3: Verify**

```bash
cd ~/src/hermes/hermes-agent-docs && git log --oneline origin/main..HEAD
```
Expected: empty (all commits pushed).

---

## Self-review checklist

- [x] Spec coverage — every section of `RELEASE_v0.11.0.md` maps to one of Tasks 1–9.
- [x] No placeholders — every step contains the actual command or content.
- [x] File-path consistency — every doc-repo file referenced exists in the listing or is in the "Files (Create:)" set.
- [x] Release boundary respected — only changes within `v2026.4.16..v2026.4.23` are normative; unreleased `main` work is excluded.
- [x] Parallelism clearly bounded to read-only audit subagents; edits centralized in this conversation.
- [x] Five-commit plan for Task 12 keeps git history navigable.
- [x] Audit artifact filenames match the existing v2026.4.16 set.

## Execution mode

This plan will be executed inline (not via subagent-driven-development) because:
- Tasks 1–9 are themselves subagent dispatches (parallel `Agent` calls).
- Tasks 10–15 are local edits + git ops in this repo, well-suited to inline work with checkpoints.
