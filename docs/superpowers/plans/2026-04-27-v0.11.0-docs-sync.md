# v0.11.0 Documentation Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bring `~/src/hermes/hermes-agent-docs` fully in sync with the released v0.11.0 (v2026.4.23, April 23, 2026) surface of `~/src/hermes/hermes-agent`, keeping the focus on shipped functionality through that tag — no unreleased/work-in-progress content.

**Architecture:** Five parallel research agents validated five non-overlapping doc clusters against `RELEASE_v0.11.0.md` and source. Their findings are consolidated here as ordered tasks: changelog first (it gates correctness of the rest), then highest-priority misleading docs (delegation/plugins/image-generation), then catalog/provider drift, then long-tail polish. Each task is one logical commit.

**Tech Stack:** Markdown (Docusaurus-flavored), git. No code generation. Source of truth: `~/src/hermes/hermes-agent` at tag `v2026.4.23` and `RELEASE_v0.11.0.md` at the repo root.

**Scope notes:**
- Released versions only — through v0.11.0. Do not document features merged after the tag.
- `RELEASE_v0.11.0.md` is the primary authority; canonical website docs at `~/src/hermes/hermes-agent/website/docs/` are the secondary mirror to compare against.
- A known release-notes inconsistency surfaced in research: notes say "5 honcho tools"; source exposes 4 (`honcho_context`, `honcho_search`, `honcho_profile`, `honcho_conclude`). Trust source — document 4.
- The five validation reports are preserved in the agent task outputs at `/private/tmp/claude-501/-Users-mudrii-src-hermes/94926239-2e1e-42ed-aa8d-166e247fe738/tasks/{aaad54ffbee20a803,a5dc98dbd13395fcb,a4ca5b30264a8d349,aca7d1f0cd3bfe73d,ad832346bde2fd1d6}.output` and serve as the line-level edit index for each file below.

---

## Phase 0 — Setup

### Task 0.1: Branch and snapshot

**Files:** none (git only)

- [ ] **Step 1:** Verify clean tree

```bash
cd ~/src/hermes/hermes-agent-docs
git status
git fetch origin
git log -1 --oneline
```

- [ ] **Step 2:** Pull latest main

```bash
git pull --ff-only origin main
```

- [ ] **Step 3:** Confirm authoritative release artifact is readable

```bash
test -r ~/src/hermes/hermes-agent/RELEASE_v0.11.0.md && echo "release notes present"
git -C ~/src/hermes/hermes-agent describe --tags --abbrev=0
```

Expected: `v2026.4.23` (or higher tag with v0.11 release notes file).

---

## Phase 1 — Changelog (gating commit)

### Task 1.1: Update changelog.md to v0.11.0

**Files:**
- Modify: `changelog.md` (header line 5; insert section before existing `## v0.10.0 -- April 16, 2026` heading)

- [ ] **Step 1:** Bump header

Replace:
```
**Current stable release: v0.10.0** (v2026.4.16, April 16, 2026)
```
with:
```
**Current stable release: v0.11.0** (v2026.4.23, April 23, 2026)
```

- [ ] **Step 2:** Insert v0.11.0 section directly above the existing `## v0.10.0 -- April 16, 2026` heading

Use the draft section produced by the changelog research agent (full text in agent output `aaad54ffbee20a803`, "DRAFT: v0.11.0 changelog.md section"). Key invariants to preserve:
- One-paragraph summary opening (TUI rewrite, transports, Bedrock, 5 new providers, QQBot, plugin surface, GPT-5.5).
- Highlights bullets covering: Ink TUI, Transport ABC + Bedrock, 5 new inference paths, GPT-5.5 over Codex, QQBot, plugin surface expansion, `/steer`, shell hooks, webhook direct-delivery, smarter delegation, auxiliary models UI, dashboard plugin system + theme switching, dashboard polish, cron `wakeAgent` + per-job toolsets, dynamic shell completion, `--ignore-user-config` + `--ignore-rules`, light-mode skins, `hermes skills reset`, Doctor "Command Installation" check, new skills, `xitter` → `xurl` rename.
- Delivery notes: 1,556 commits, 761 PRs, 1,314 files, 224,174 insertions; top contributors block.
- Compare link: `v2026.4.16...v2026.4.23`.

- [ ] **Step 3:** Verify locally that v0.10.0 and earlier sections are untouched

```bash
git diff changelog.md | head -100
git diff changelog.md | grep -c "^-## v0\." # should be 0 — no removed older sections
```

- [ ] **Step 4:** Commit

```bash
git add changelog.md
git commit -m "docs(changelog): add v0.11.0 release section"
```

---

## Phase 2 — Top-of-funnel + actively misleading docs

These docs are either the first thing readers hit (README, top-level providers/architecture) or contain text that contradicts v0.11.0 reality. Fix early so later cross-references land on accurate pages.

### Task 2.1: README.md version + platform count + headline features

**Files:**
- Modify: `README.md`

- [ ] **Step 1:** Bump current-version line from `v0.10.0 (v2026.4.16)` to `v0.11.0 (v2026.4.23)`.
- [ ] **Step 2:** Update platform count from 15 to 17 (adds QQBot; restores parity with messaging index).
- [ ] **Step 3:** Add brief mention of `hermes --tui`, transport ABC, native Bedrock, and the new providers (NIM, Arcee, Step Plan, Vercel ai-gateway, Gemini CLI OAuth, GPT-5.5 over Codex OAuth).
- [ ] **Step 4:** Commit `docs(readme): bump to v0.11.0 and refresh feature list`.

### Task 2.2: architecture.md — full v0.11.0 alignment

**Files:**
- Modify: `architecture.md`

- [ ] **Step 1:** Replace "released v0.8.0 surface (v2026.4.8)" framing line with v0.11.0 framing.
- [ ] **Step 2:** Replace the inline "3 API modes" block with the new transport-layer section: `agent/transports/base.py` defines `ProviderTransport` ABC with abstract `convert_messages`, `convert_tools`, `build_kwargs`, `normalize_response` and optional `validate_response`, `extract_cache_stats`, `map_finish_reason`. Concrete implementations: `AnthropicTransport`, `ChatCompletionsTransport`, `ResponsesApiTransport`, `BedrockTransport`. Make explicit that the transport layer owns format conversion only — streaming, retries, cache, and credential refresh remain on `AIAgent`.
- [ ] **Step 3:** Add v0.11.0 deltas: orchestrator role + `max_spawn_depth`, cross-agent file-state coordination, auto-prune + VACUUM at startup, `/steer`, auto-continue interrupted work, PLATFORM_HINTS for Matrix/Mattermost/Feishu.
- [ ] **Step 4:** Commit `docs(architecture): rewrite for v0.11.0 transport layer and orchestrator delegation`.

### Task 2.3: developer-guide/architecture.md mirror

**Files:**
- Modify: `developer-guide/architecture.md`

- [ ] **Step 1:** Replace "3 API modes" section with the same transport-layer description.
- [ ] **Step 2:** Add a forward link to `developer-guide/transport-layer.md` (created in Task 4.1).
- [ ] **Step 3:** Name the four concrete transports.
- [ ] **Step 4:** Commit `docs(dev-guide): align architecture page with transport ABC`.

### Task 2.4: delegation.md — orchestrator + max_spawn_depth

**Files:**
- Modify: `delegation.md`

- [ ] **Step 1:** Remove "MAX_DEPTH = 2 fixed" / "no nested delegation" / "Depth Limit of 2" assertions.
- [ ] **Step 2:** Replace with: subagents have an explicit `role` (worker | orchestrator). Orchestrators retain `delegate_task` when `delegation.max_spawn_depth > 1` (range 1-3, default 1). Leaf workers cannot delegate. Disable globally with `delegation.orchestrator_enabled: false`.
- [ ] **Step 3:** Document the cross-agent file-state coordination layer (concurrent siblings share filesystem state; no clobbered edits).
- [ ] **Step 4:** Add config example block with `delegation.max_concurrent_children: 3`, `delegation.max_spawn_depth: 1`, `delegation.orchestrator_enabled: true`.
- [ ] **Step 5:** Commit `docs(delegation): document v0.11.0 orchestrator role and max_spawn_depth`.

### Task 2.5: plugins.md — surface expansion

**Files:**
- Modify: `plugins.md`

- [ ] **Step 1:** Add `ctx.register_command()` (slash-command registration) section with example.
- [ ] **Step 2:** Add `ctx.dispatch_tool()` (programmatic tool invocation from plugins) section.
- [ ] **Step 3:** Document blocking semantics for `pre_tool_call`: returning `{"action": "block", "message": "..."}` vetoes execution.
- [ ] **Step 4:** Add `transform_tool_result` and `transform_terminal_output` hook docs.
- [ ] **Step 5:** Document pluggable image_gen backends with the `plugins/image_gen/{openai,openai-codex,xai}/` layout.
- [ ] **Step 6:** Document namespaced skill registration via plugin context.
- [ ] **Step 7:** Document shell-hook bridge integration (cross-link to `hooks.md`).
- [ ] **Step 8:** Add `subagent_stop` and `pre_gateway_dispatch` lifecycle hooks to the lifecycle table.
- [ ] **Step 9:** Add note on the bundled opt-in `plugins/disk-cleanup/` reference plugin.
- [ ] **Step 10:** Add dashboard plugin tabs/widgets brief (cross-link `dashboard-plugins.md`).
- [ ] **Step 11:** Commit `docs(plugins): document v0.11.0 plugin surface expansion`.

### Task 2.6: hooks.md — three hook systems

**Files:**
- Modify: `hooks.md`

- [ ] **Step 1:** Add an opening section that names the three hook systems: gateway lifecycle hooks, plugin hooks, and shell hooks (new in v0.11.0).
- [ ] **Step 2:** Add full **Shell Hooks** section: `hooks:` block in `cli-config.yaml`, JSON wire protocol over stdin/stdout, allowlist consent model, `hermes hooks` CLI subcommands. Mirror canonical `~/src/hermes/hermes-agent/website/docs/user-guide/features/hooks.md`.
- [ ] **Step 3:** Add `subagent_stop` and `pre_gateway_dispatch` lifecycle hooks.
- [ ] **Step 4:** Document blocking `pre_tool_call` return shape (parity with `plugins.md`).
- [ ] **Step 5:** Add `transform_tool_result` and `transform_terminal_output` rows.
- [ ] **Step 6:** Update `post_tool_call` signature to include `duration_ms: int`.
- [ ] **Step 7:** Remove `pre_api_request` / `post_api_request` rows that no longer exist in source.
- [ ] **Step 8:** Add `on_session_finalize` and `on_session_reset` rows.
- [ ] **Step 9:** Commit `docs(hooks): document shell hooks and v0.11.0 lifecycle additions`.

### Task 2.7: image-generation.md — pluggable backends

**Files:**
- Modify: `image-generation.md`

- [ ] **Step 1:** Replace single-backend FLUX 2 Pro narrative with a "pluggable backend" model.
- [ ] **Step 2:** Add multi-model FAL picker, Recraft V4 Pro (was V3), Nano Banana Pro (was Nano Banana), GPT Image 2 (OpenAI), xAI grok-imagine, openai-codex plugin (gpt-image-2 via Codex OAuth).
- [ ] **Step 3:** Document the `plugins/image_gen/{openai,openai-codex,xai}/` plugin architecture and how to point at a backend in config.
- [ ] **Step 4:** Add per-backend config schemas.
- [ ] **Step 5:** Note that the Nous Tool Gateway only routes the FAL backend — direct keys are required for Recraft/Nano Banana/etc.
- [ ] **Step 6:** Commit `docs(image-generation): document pluggable backend system`.

---

## Phase 3 — Provider catalog and configuration drift

These pages are consulted on every "what providers does Hermes support" question and on every config edit. They have heavy v0.11.0 drift (5 new inference paths, ABC layer, expanded knobs).

### Task 3.1: providers.md and integrations/providers.md (reconcile + rewrite)

**Files:**
- Modify: `providers.md`
- Modify: `integrations/providers.md`

- [ ] **Step 1:** Pick `providers.md` as the canonical page; reduce `integrations/providers.md` to a redirect/stub pointing at it (keep frontmatter).
- [ ] **Step 2:** Update model strings: `claude-sonnet-4-6` / `claude-opus-4.6` → `claude-opus-4.7`; `gpt-5.4` → `gpt-5.5`.
- [ ] **Step 3:** Add provider rows: NVIDIA NIM (`nvidia`, `NVIDIA_API_KEY`/`NVIDIA_BASE_URL`), Step Plan, Google Gemini CLI OAuth (`google-gemini-cli`, `HERMES_GEMINI_PROJECT_ID`), Vercel ai-gateway (with pricing/dynamic discovery), Ollama Cloud (`ollama-cloud`, `OLLAMA_API_KEY`), xAI Grok Responses API (`xai`/`grok`), Mistral, Azure Foundry (`azure-foundry`), Qwen Portal OAuth (`qwen-oauth`, `HERMES_QWEN_BASE_URL`).
- [ ] **Step 4:** Replace the v0.10.0 Bedrock subsection with the **native Converse API** description: IAM auth, region-aware discovery, inference profiles (`us.` / `global.` prefixes), Guardrails support, boto3 timeout note.
- [ ] **Step 5:** Add Codex OAuth GPT-5.5 row + live model discovery note.
- [ ] **Step 6:** Add Kimi K2.5/K2.6, Xiaomi MiMo v2.5/v2.5-pro entries.
- [ ] **Step 7:** Replace any `api_key_env:` references in custom-endpoint examples with `key_env:`.
- [ ] **Step 8:** Document per-provider/per-model `request_timeout_seconds`, `stale_timeout_seconds`, `timeout_seconds`, and `agent.api_max_retries`.
- [ ] **Step 9:** Commit `docs(providers): rewrite for v0.11.0 transport-aware provider catalog`.

### Task 3.2: configuration.md — v0.11.0 knobs

**Files:**
- Modify: `configuration.md`

- [ ] **Step 1:** Rename "Streaming Timeouts" section to "API Timeouts"; add `stale_timeout_seconds`, `request_timeout_seconds`, `timeout_seconds` rows.
- [ ] **Step 2:** Replace `auxiliary.flush_memories` with `auxiliary.approval` and `auxiliary.title_generation`. Add note that `auto` routing now defaults to the main model.
- [ ] **Step 3:** Expand auxiliary providers list to include `nvidia`, `xai`, `ollama-cloud`, `bedrock`, `ai-gateway`, `azure-foundry`, `huggingface`, `xiaomi`, `kilocode`, `opencode-zen`, `opencode-go`, `qwen-oauth`, `google-gemini-cli`, `gemini`, `minimax-cn`, `deepseek`.
- [ ] **Step 4:** Add `gemini` and `xai` to the TTS provider list with config blocks; ensure `mistral` block exists.
- [ ] **Step 5:** Remove the `wiki` skill key example (skill removed). Replace any remaining `wiki` references.
- [ ] **Step 6:** Update Docker volume example: `/outputs` → `/output`. Add `MEDIA:` workflow note.
- [ ] **Step 7:** Update Docker container lifecycle paragraph to reflect v0.11.0 single-container reuse across `/new`, `/reset`, and `delegate_task`.
- [ ] **Step 8:** Add `code_execution.mode: project | strict` (default `project`) section.
- [ ] **Step 9:** Add **Tool Output Truncation Limits** section with `tool_output.max_bytes`, `tool_output.max_lines`, `tool_output.max_line_length`.
- [ ] **Step 10:** Add `delegation.max_concurrent_children`, `delegation.max_spawn_depth`, `delegation.orchestrator_enabled`.
- [ ] **Step 11:** Add `voice.beep_enabled`, `streaming.fresh_final_after_seconds`.
- [ ] **Step 12:** Add `browser.cdp_url`, `browser.dialog_policy`, `browser.dialog_timeout_s`.
- [ ] **Step 13:** Add `auxiliary.session_search.max_concurrency` and `extra_body`.
- [ ] **Step 14:** Remove "Image Generation use_gateway" block (removed in v0.11.0); also remove web `use_gateway`.
- [ ] **Step 15:** Add `agent.api_max_retries`.
- [ ] **Step 16:** Commit `docs(configuration): document v0.11.0 config surface`.

### Task 3.3: reference/cli-commands.md

**Files:**
- Modify: `reference/cli-commands.md`

- [ ] **Step 1:** Add global flags: `--ignore-user-config`, `--ignore-rules`, `--tui`, `--dev`.
- [ ] **Step 2:** Add `hermes slack` runner subcommand row.
- [ ] **Step 3:** Expand `--provider` value list to include `google-gemini-cli`, `kimi-coding-cn`, `alibaba`, `deepseek`, `nvidia`, `ollama-cloud`, `xai`/`grok`, `qwen-oauth`, `bedrock`, `opencode-zen`, `opencode-go`, `ai-gateway`, `azure-foundry`.
- [ ] **Step 4:** Add `hermes setup --quick` and `hermes setup --reconfigure` flags.
- [ ] **Step 5:** Remove duplicate rows for `hermes debug`, `hermes backup`, `hermes import`.
- [ ] **Step 6:** Add `hermes skills install <url>` form and registry sources (`--source skills-sh`, `--source well-known`).
- [ ] **Step 7:** Add `hermes skills reset` subcommand.
- [ ] **Step 8:** Commit `docs(cli-commands): document v0.11.0 CLI surface`.

### Task 3.4: reference/environment-variables.md

**Files:**
- Modify: `reference/environment-variables.md`

- [ ] **Step 1:** Add provider keys: `NVIDIA_API_KEY`/`NVIDIA_BASE_URL`, `OLLAMA_API_KEY`/`OLLAMA_BASE_URL`, `XAI_API_KEY`/`XAI_BASE_URL`, `MISTRAL_API_KEY`, `AWS_REGION`, `AWS_PROFILE`, `BEDROCK_BASE_URL`, `HERMES_QWEN_BASE_URL`, `AZURE_FOUNDRY_*`, `HERMES_GEMINI_PROJECT_ID`, `NOUS_BASE_URL`.
- [ ] **Step 2:** Add tuning keys: `HERMES_API_CALL_STALE_TIMEOUT`, `DELEGATION_MAX_CONCURRENT_CHILDREN`, `HERMES_TUI`, `HERMES_TUI_DIR`.
- [ ] **Step 3:** Update defaults: `MINIMAX_BASE_URL` and `MINIMAX_CN_BASE_URL` now point to `/anthropic` endpoints; refresh `DASHSCOPE_BASE_URL`.
- [ ] **Step 4:** Add Discord vars: `DISCORD_ALLOWED_ROLES`, `DISCORD_ALLOWED_CHANNELS`, `DISCORD_PROXY`, `DISCORD_COMMAND_SYNC_POLICY`, `DISCORD_ALLOW_MENTION_*`.
- [ ] **Step 5:** Add Matrix `DEVICE_ID`, `MATRIX_REACTIONS`; Telegram `TELEGRAM_REPLY_TO_MODE`.
- [ ] **Step 6:** Rename QQ vars: `QQ_HOME_CHANNEL` → `QQBOT_HOME_CHANNEL`; add `QQBOT_HOME_CHANNEL_NAME`, `QQ_SANDBOX`.
- [ ] **Step 7:** Refresh `HERMES_INFERENCE_PROVIDER` allowed-values list.
- [ ] **Step 8:** Commit `docs(env-vars): document v0.11.0 environment variables`.

### Task 3.5: reference/slash-commands.md

**Files:**
- Modify: `reference/slash-commands.md`

- [ ] **Step 1:** Add `/steer` row (mid-run agent nudges).
- [ ] **Step 2:** Add `/agents` (with alias `/tasks`), `/copy`, `/terminal-setup`, `/gquota`.
- [ ] **Step 3:** Mark `/btw` as alias of `/background` (alongside `/bg`); de-duplicate the row.
- [ ] **Step 4:** Remove duplicate rows for `/snapshot`, `/fast`, `/debug`.
- [ ] **Step 5:** Add Notes-section entries for `/steer`, `/agents`, `/copy`, `/gquota`, `/terminal-setup`.
- [ ] **Step 6:** Commit `docs(slash-commands): add /steer and v0.11.0 commands`.

### Task 3.6: fallback-providers.md

**Files:**
- Modify: `fallback-providers.md`

- [ ] **Step 1:** Add NVIDIA, Ollama Cloud, Google Gemini CLI OAuth, Google AI Studio, xAI/Grok, AWS Bedrock, Qwen Portal OAuth rows.
- [ ] **Step 2:** Replace `api_key_env` with `key_env` in custom-endpoint examples.
- [ ] **Step 3:** Replace "one-shot fallback" wording with "per-turn" semantics — primary is restored each turn.
- [ ] **Step 4:** Replace `auxiliary.flush_memories` with `auxiliary.approval` + `auxiliary.title_generation`.
- [ ] **Step 5:** Add `session_search.max_concurrency` and `extra_body` knobs.
- [ ] **Step 6:** Commit `docs(fallback-providers): refresh v0.11.0 fallback semantics and providers`.

### Task 3.7: credential-pools.md

**Files:**
- Modify: `credential-pools.md`

- [ ] **Step 1:** Add **"Delegation & Subagent Sharing"** section — parent pool is shared with `delegate_task` children.
- [ ] **Step 2:** Remove the `credential_pool_strategies` JSON example block (no longer in canonical).
- [ ] **Step 3:** Commit `docs(credential-pools): document subagent leasing`.

---

## Phase 4 — New file + developer-guide cluster

### Task 4.1: NEW developer-guide/transport-layer.md

**Files:**
- Create: `developer-guide/transport-layer.md`

- [ ] **Step 1:** Write the file. Cover:
  - The ABC contract: 4 abstract methods (`convert_messages`, `convert_tools`, `build_kwargs`, `normalize_response`) + 3 optional (`validate_response`, `extract_cache_stats`, `map_finish_reason`).
  - Explicit non-responsibilities: streaming, retries, cache, credential refresh — those stay on `AIAgent`.
  - The four concrete subclasses: `AnthropicTransport`, `ChatCompletionsTransport`, `ResponsesApiTransport`, `BedrockTransport`.
  - How `api_mode` maps to a transport instance.
  - The `NormalizedResponse` type.
  - Adding a new transport: ABC subclass → register → wire into provider catalog.
  - Source path: `agent/transports/`.
- [ ] **Step 2:** Add Docusaurus frontmatter (title, description, sidebar_position).
- [ ] **Step 3:** Add cross-links to `provider-runtime.md`, `agent-loop.md`, `architecture.md`.
- [ ] **Step 4:** Commit `docs(dev-guide): add transport-layer reference`.

### Task 4.2: developer-guide/provider-runtime.md

**Files:**
- Modify: `developer-guide/provider-runtime.md`

- [ ] **Step 1:** Add the 5 new inference paths (NIM, Step Plan, Gemini CLI OAuth, Vercel ai-gateway, GPT-5.5 over Codex), AWS Bedrock Converse, and the `azure-foundry` / `qwen-oauth` flavors.
- [ ] **Step 2:** Add a forward link to `transport-layer.md`.
- [ ] **Step 3:** Commit `docs(dev-guide): refresh provider-runtime for v0.11.0 providers`.

### Task 4.3: developer-guide/agent-loop.md

**Files:**
- Modify: `developer-guide/agent-loop.md`

- [ ] **Step 1:** Replace "3 API modes inline dispatch" with transport-layer dispatch reference.
- [ ] **Step 2:** Add `/steer` mid-run nudge integration point in the loop.
- [ ] **Step 3:** Add orchestrator role + `max_spawn_depth` interaction with the loop.
- [ ] **Step 4:** Add auto-continue interrupted work.
- [ ] **Step 5:** Add activity heartbeats.
- [ ] **Step 6:** Add fallback-to-main on 503/404.
- [ ] **Step 7:** Commit `docs(dev-guide): align agent-loop with v0.11.0`.

### Task 4.4: developer-guide/adding-providers.md

**Files:**
- Modify: `developer-guide/adding-providers.md`

- [ ] **Step 1:** Insert "Pick or implement a `ProviderTransport`" step before the catalog-registration step.
- [ ] **Step 2:** Add `BedrockTransport` example at the relevant step.
- [ ] **Step 3:** Map `api_mode` → transport class.
- [ ] **Step 4:** Commit `docs(dev-guide): teach adding-providers via transport ABC`.

### Task 4.5: developer-guide/session-storage.md

**Files:**
- Modify: `developer-guide/session-storage.md`

- [ ] **Step 1:** Add **auto-prune + VACUUM at startup** behavior.
- [ ] **Step 2:** Add FTS5 rebuild-across-upgrades semantics.
- [ ] **Step 3:** Commit `docs(dev-guide): document session auto-prune and FTS5 rebuild`.

### Task 4.6: contributing.md plugin-hooks list

**Files:**
- Modify: `contributing.md`

- [ ] **Step 1:** Update plugin-hooks list with: `register_command`, `dispatch_tool`, `pre_tool_call` veto, `transform_tool_result`, `transform_terminal_output`, image_gen backends, dashboard tabs.
- [ ] **Step 2:** Commit `docs(contributing): refresh plugin extension points`.

---

## Phase 5 — Memory / sessions / sec / skins / streaming / dashboard

### Task 5.1: memory.md / memory-providers.md dedupe

**Files:**
- Modify: `memory.md`
- Modify: `memory-providers.md`

- [ ] **Step 1:** Pick `memory-providers.md` as canonical (more descriptive name).
- [ ] **Step 2:** Reduce `memory.md` to a 5-line stub with frontmatter `redirect_to: /memory-providers` (or the Docusaurus-equivalent redirect mechanism in this repo) and a link.
- [ ] **Step 3:** Add Hindsight retain-metadata note and pointer to overhauled `honcho.md` in `memory-providers.md`.
- [ ] **Step 4:** Commit `docs(memory): dedupe memory.md and memory-providers.md`.

### Task 5.2: honcho.md correction + v0.11.0 overhaul section

**Files:**
- Modify: `honcho.md`

- [ ] **Step 1:** Fix path: `plugins/memory/honcho/` → `honcho_integration/` (`__init__.py`, `cli.py`, `client.py`, `session.py`).
- [ ] **Step 2:** Confirm "four tools" wording — `honcho_context`, `honcho_search`, `honcho_profile`, `honcho_conclude` (release notes' "5 tools" claim is incorrect; do not propagate).
- [ ] **Step 3:** Add v0.11.0 Honcho overhaul subsection: auto-conclude on session end, profile cache, search-tool latency improvements.
- [ ] **Step 4:** Commit `docs(honcho): fix source path and add v0.11.0 overhaul section`.

### Task 5.3: sessions.md

**Files:**
- Modify: `sessions.md`

- [ ] **Step 1:** Add auto-prune + VACUUM at startup behavior (cross-link to dev-guide).
- [ ] **Step 2:** Add `/steer` interaction with active sessions (note that nudges land between turns and don't break prompt cache).
- [ ] **Step 3:** Add orchestrator role / cross-agent file coordination semantics in lineage.
- [ ] **Step 4:** Commit `docs(sessions): document v0.11.0 lifecycle additions`.

### Task 5.4: security.md

**Files:**
- Modify: `security.md`

- [ ] **Step 1:** Replace v0.8.0 baseline framing with v0.11.0.
- [ ] **Step 2:** Add SECURITY.md addition (#10532).
- [ ] **Step 3:** Add private/internal URL toggle (#14166).
- [ ] **Step 4:** Commit `docs(security): refresh for v0.11.0`.

### Task 5.5: skins.md

**Files:**
- Modify: `skins.md`

- [ ] **Step 1:** Add light-theme preset.
- [ ] **Step 2:** Add live-switching dashboard themes (cross-link `web-dashboard.md`).
- [ ] **Step 3:** Commit `docs(skins): add v0.11.0 light theme + dashboard live switching`.

### Task 5.6: live-model-switching.md

**Files:**
- Modify: `live-model-switching.md`

- [ ] **Step 1:** Add auxiliary models UI screen.
- [ ] **Step 2:** Document main-model-first auto routing default.
- [ ] **Step 3:** Add fallback-to-main on 503/404.
- [ ] **Step 4:** Commit `docs(live-model-switching): document auxiliary UI and main-model defaults`.

### Task 5.7: streaming.md

**Files:**
- Modify: `streaming.md`

- [ ] **Step 1:** Add streaming cursor sanitization fixes.
- [ ] **Step 2:** Add transport-layer normalization note (with cross-link).
- [ ] **Step 3:** Commit `docs(streaming): note v0.11.0 cursor fixes and transport normalization`.

### Task 5.8: web-dashboard.md

**Files:**
- Modify: `web-dashboard.md`

- [ ] **Step 1:** Replace flat top-bar layout description with **react-router sidebar** layout.
- [ ] **Step 2:** Add mobile-responsive note.
- [ ] **Step 3:** Add **i18n EN+ZH** with header switcher.
- [ ] **Step 4:** Add Vercel deployment note.
- [ ] **Step 5:** Add one-click update + gateway restart buttons.
- [ ] **Step 6:** Add real per-session API call tracking.
- [ ] **Step 7:** Expand theme system description to cover live switching of colors+fonts+layout+density.
- [ ] **Step 8:** Commit `docs(web-dashboard): document v0.11.0 dashboard polish`.

---

## Phase 6 — Tools / browser / voice / cron

### Task 6.1: tools.md

**Files:**
- Modify: `tools.md`

- [ ] **Step 1:** Add `browser_cdp` raw DevTools Protocol passthrough tool.
- [ ] **Step 2:** Update `image_generate` description to "pluggable backend" (cross-link `image-generation.md`).
- [ ] **Step 3:** Update cron tool description with `wakeAgent` and per-job `enabled_toolsets`.
- [ ] **Step 4:** Replace "max depth 2 fixed" delegation framing with orchestrator/`max_spawn_depth`.
- [ ] **Step 5:** Add code execution `project`/`strict` modes (default `project`).
- [ ] **Step 6:** Add patch tool fuzzy "did you mean?" suggestion.
- [ ] **Step 7:** Add new TTS/STT providers (xAI Grok STT, Gemini TTS, KittenTTS, xAI TTS).
- [ ] **Step 8:** Add CLI record beep (`voice.beep_enabled`).
- [ ] **Step 9:** Add webhook direct-delivery (zero-LLM forwarding) cross-link.
- [ ] **Step 10:** Add API server SSE tool events + inline image input cross-link.
- [ ] **Step 11:** Commit `docs(tools): document v0.11.0 tool surface`.

### Task 6.2: toolsets.md

**Files:**
- Modify: `toolsets.md`

- [ ] **Step 1:** Update `image_gen` toolset description to multi-backend.
- [ ] **Step 2:** Add `browser_cdp` to `browser` toolset.
- [ ] **Step 3:** Refresh TTS toolset enumeration (add Mistral, Gemini TTS, KittenTTS, xAI TTS).
- [ ] **Step 4:** Add `hermes-qqbot` to platform toolsets.
- [ ] **Step 5:** Add cron toolset note about `enabled_toolsets` per-job override.
- [ ] **Step 6:** Commit `docs(toolsets): refresh for v0.11.0`.

### Task 6.3: skills.md

**Files:**
- Modify: `skills.md`

- [ ] **Step 1:** Add `hermes skills reset` command.
- [ ] **Step 2:** Add `config.skills.guard_agent_created` opt-in flag.
- [ ] **Step 3:** Add namespaced skill registration via plugins note.
- [ ] **Step 4:** Add `xitter` → `xurl` rename note.
- [ ] **Step 5:** Add fuzzy `@<skill>` completion mention.
- [ ] **Step 6:** Add new bundled skills: `concept-diagrams`, `architecture-diagram`, `pixel-art`, `baoyu-comic`, `baoyu-infographic`, `page-agent`, `drug-discovery`, `touchdesigner-mcp`, `adversarial-ux-test`.
- [ ] **Step 7:** Add direct HTTPS install example: `hermes skills install https://example.com/SKILL.md` and `/skills install ... --name my-skill`.
- [ ] **Step 8:** Commit `docs(skills): document v0.11.0 skill surface`.

### Task 6.4: browser.md

**Files:**
- Modify: `browser.md`

- [ ] **Step 1:** Add `browser_cdp` raw CDP passthrough tool to "Available Tools".
- [ ] **Step 2:** Add Camofox hardening notes (managed persistence, VNC discovery polish).
- [ ] **Step 3:** Commit `docs(browser): document browser_cdp and Camofox hardening`.

### Task 6.5: code-execution.md

**Files:**
- Modify: `code-execution.md`

- [ ] **Step 1:** Add `code_execution.mode: project | strict` config block (default `project`).
- [ ] **Step 2:** Document behavioral differences between modes.
- [ ] **Step 3:** Commit `docs(code-execution): document project/strict modes`.

### Task 6.6: tts.md

**Files:**
- Modify: `tts.md`

- [ ] **Step 1:** Add **Google Gemini TTS** provider.
- [ ] **Step 2:** Add **KittenTTS** local provider.
- [ ] **Step 3:** Add **xAI TTS** provider.
- [ ] **Step 4:** Add **xAI Grok STT** provider to STT auto-detect chain.
- [ ] **Step 5:** Add CLI `voice.record_beep` toggle.
- [ ] **Step 6:** Commit `docs(tts): add v0.11.0 voice providers`.

### Task 6.7: voice-mode.md

**Files:**
- Modify: `voice-mode.md`

- [ ] **Step 1:** Same provider additions as `tts.md` (Mistral if missing, Gemini TTS, KittenTTS, xAI TTS, Grok STT).
- [ ] **Step 2:** Add `voice.beep_enabled` config example.
- [ ] **Step 3:** Commit `docs(voice-mode): align provider list with tts.md`.

### Task 6.8: cron.md

**Files:**
- Modify: `cron.md`

- [ ] **Step 1:** Add `wakeAgent: true|false` per-job flag (controls whether scheduled run wakes agent — `false` runs the script only, no LLM tokens).
- [ ] **Step 2:** Add per-job `enabled_toolsets` field.
- [ ] **Step 3:** Update `cronjob` action signature to reflect both fields.
- [ ] **Step 4:** Commit `docs(cron): document wakeAgent and per-job toolsets`.

---

## Phase 7 — Reference catalogs

### Task 7.1: reference/tools-reference.md

**Files:**
- Modify: `reference/tools-reference.md`

- [ ] **Step 1:** Update tool count from "47 built-in tools" to the v0.11.0 count (verify against `tools/` registry).
- [ ] **Step 2:** Add `browser_cdp` row.
- [ ] **Step 3:** Update `image_generate` row to "pluggable backends".
- [ ] **Step 4:** Add new TTS/STT provider rows.
- [ ] **Step 5:** Commit `docs(tools-reference): refresh for v0.11.0`.

### Task 7.2: reference/toolsets-reference.md

**Files:**
- Modify: `reference/toolsets-reference.md`

- [ ] **Step 1:** Update `hermes-cli` count to reflect added `browser_cdp`.
- [ ] **Step 2:** Add `hermes-qqbot` platform toolset row.
- [ ] **Step 3:** Update `image_gen` description to multi-backend.
- [ ] **Step 4:** Commit `docs(toolsets-reference): refresh for v0.11.0`.

### Task 7.3: reference/skills-catalog.md

**Files:**
- Modify: `reference/skills-catalog.md`

- [ ] **Step 1:** Rename `xitter` entry to `xurl`.
- [ ] **Step 2:** Add `concept-diagrams`, `architecture-diagram`, `pixel-art`, `baoyu-comic`, `baoyu-infographic`, `page-agent`, `drug-discovery`, `touchdesigner-mcp`, `adversarial-ux-test`.
- [ ] **Step 3:** Verify each new path against `~/src/hermes/hermes-agent/skills/` tree before committing.
- [ ] **Step 4:** Commit `docs(skills-catalog): add v0.11.0 bundled skills`.

### Task 7.4: reference/optional-skills-catalog.md

**Files:**
- Modify: `reference/optional-skills-catalog.md`

- [ ] **Step 1:** Verify each existing entry against `~/src/hermes/hermes-agent/optional-skills/`.
- [ ] **Step 2:** Add any v0.11.0 additions (verify `agentmail`, `telephony`, `1password` are accurate).
- [ ] **Step 3:** Commit `docs(optional-skills-catalog): refresh for v0.11.0`.

### Task 7.5: reference/faq.md

**Files:**
- Modify: `reference/faq.md`

- [ ] **Step 1:** Replace stale model strings: `openrouter/nous/hermes-3-llama-3.1-70b` → `anthropic/claude-opus-4.7`; `anthropic/claude-sonnet-4.6` → `anthropic/claude-opus-4.7`.
- [ ] **Step 2:** Add new Q&A: "Terminal commands say `node: command not found` (or `nvm`, `pyenv`, `asdf`, …)" — explain auto-source of `~/.bashrc`, `terminal.shell_init_files`, and `terminal.auto_source_bashrc: false`.
- [ ] **Step 3:** Replace "Move Hermes Agent to a different machine" rsync workflow with `hermes backup` / `hermes import`.
- [ ] **Step 4:** Add "Moving a single profile to another machine" subsection + `hermes backup` vs `hermes profile export` comparison table.
- [ ] **Step 5:** Remove duplicate "Can I move a profile to a different machine?" Q&A.
- [ ] **Step 6:** Commit `docs(faq): refresh model examples and migration workflow`.

### Task 7.6: reference/profile-commands.md

**Files:**
- Modify: `reference/profile-commands.md`

- [ ] **Step 1:** Add Docusaurus `sidebar_position` frontmatter.
- [ ] **Step 2:** After `hermes profile create` flag table, add: "Creating a profile does **not** make that profile directory the default project/workspace directory for terminal commands. If you want a profile to start in a specific project, set `terminal.cwd` in that profile's `config.yaml`."
- [ ] **Step 3:** In `hermes profile show` section, add: "This shows the profile's Hermes home directory, not the terminal working directory. Terminal commands start from `terminal.cwd` (or the launch directory on the local backend when `cwd: \".\"`)."
- [ ] **Step 4:** Commit `docs(profile-commands): clarify cwd semantics`.

---

## Phase 8 — Messaging platforms

### Task 8.1: messaging/index.md and messaging/README.md

**Files:**
- Modify: `messaging/index.md`
- Modify: `messaging/README.md`

- [ ] **Step 1:** Fix DingTalk capability row: `— ✅ ✅ — ✅ — ✅` (Images/Files/Reactions, no Typing).
- [ ] **Step 2:** Remove `/provider` from chat-commands table.
- [ ] **Step 3:** Add **Queue vs interrupt vs steer (busy_input_mode)** subsection: `display.busy_input_mode` config, `onboarding.seen.busy_input_prompt` flag.
- [ ] **Step 4:** Update architecture diagram in README.md to add QQ/QQBot node.
- [ ] **Step 5:** Commit `docs(messaging): refresh index/README for v0.11.0`.

### Task 8.2: messaging/gateway.md (top-level gateway.md)

**Files:**
- Modify: `gateway.md`

- [ ] **Step 1:** Bump framing line from "released through v0.10.0 (v2026.4.16)" to "released through v0.11.0 (v2026.4.23)".
- [ ] **Step 2:** Add **QQ** node to the architecture mermaid (17th platform).
- [ ] **Step 3:** Remove `/provider` row from Chat Commands table; add `/steer` row; add `/skill` Discord command group reference.
- [ ] **Step 4:** Add `hermes-feishu`, `hermes-wecom`, `hermes-weixin`, `hermes-bluebubbles`, `hermes-qqbot` to Platform-Specific Tool Configuration table.
- [ ] **Step 5:** Add v0.9, v0.10, v0.11 entries to the changelog section.
- [ ] **Step 6:** Add **Gateway proxy mode** (forward to remote API server) section.
- [ ] **Step 7:** Add **Per-channel ephemeral prompts** subsection.
- [ ] **Step 8:** Add **Plugin slash commands surfaced natively on all platforms**.
- [ ] **Step 9:** Add **MEDIA: tag** support for documents/archives + `.pdf`.
- [ ] **Step 10:** Add `--all` flag for `gateway start`/`restart`.
- [ ] **Step 11:** Add **Notify active sessions on shutdown**.
- [ ] **Step 12:** Add **Block agent self-destructing the gateway**.
- [ ] **Step 13:** Add **busy_input_mode** (interrupt/queue/steer) and onboarding tip.
- [ ] **Step 14:** Add **Auto-continue interrupted work after restart** + **activity heartbeats**.
- [ ] **Step 15:** Add **PLATFORM_HINTS** for Matrix/Mattermost/Feishu.
- [ ] **Step 16:** Commit `docs(gateway): rewrite for v0.11.0 gateway surface`.

### Task 8.3: messaging/discord.md (major rewrite)

**Files:**
- Modify: `messaging/discord.md`

- [ ] **Step 1:** Add **Role-Based Access Control** section: `DISCORD_ALLOWED_ROLES` env var.
- [ ] **Step 2:** Replace `discord.slash_commands: false` with `DISCORD_COMMAND_SYNC_POLICY` (`safe`/`bulk`/`off`).
- [ ] **Step 3:** Add `DISCORD_ALLOWED_CHANNELS`.
- [ ] **Step 4:** Add **Mention control**: `DISCORD_ALLOW_MENTION_EVERYONE/ROLES/USERS/REPLIED_USER` and `discord.allow_mentions:` config block.
- [ ] **Step 5:** Add `DISCORD_PROXY`.
- [ ] **Step 6:** Add `HERMES_DISCORD_TEXT_BATCH_DELAY_SECONDS` / `_SPLIT_DELAY_SECONDS`.
- [ ] **Step 7:** Add **Forum Channels** section (auto-detection, thread naming, three-layer cache).
- [ ] **Step 8:** Add `send_animation` native GIF playback.
- [ ] **Step 9:** Add `send_message` Discord media attachments.
- [ ] **Step 10:** Add `/skill` command group.
- [ ] **Step 11:** Clarify auto-thread bypass for free-response/no-thread channels.
- [ ] **Step 12:** Commit `docs(discord): rewrite for v0.11.0`.

### Task 8.4: messaging/dingtalk.md (major rewrite)

**Files:**
- Modify: `messaging/dingtalk.md`

- [ ] **Step 1:** Bump header from "v0.2.0 through v0.8.0".
- [ ] **Step 2:** Update install instructions: add `alibabacloud-dingtalk` (or `pip install "hermes-agent[dingtalk]"`).
- [ ] **Step 3:** Add `require_mention` + `allowed_users` gating.
- [ ] **Step 4:** Add **QR-code device-flow setup** with openClaw branding disclosure.
- [ ] **Step 5:** Add AI Cards streaming, emoji reactions, media handling.
- [ ] **Step 6:** Add `group_sessions_per_user` reference; bring env-var table to parity with canonical.
- [ ] **Step 7:** Commit `docs(dingtalk): rewrite for v0.11.0`.

### Task 8.5: messaging/wecom.md (major rewrite)

**Files:**
- Modify: `messaging/wecom.md`

- [ ] **Step 1:** Replace prose with the canonical-style rewrite.
- [ ] **Step 2:** Add **Scan-to-Create** wizard (auto-creates bot via QR scan).
- [ ] **Step 3:** Expand manual setup steps and config tables.
- [ ] **Step 4:** Commit `docs(wecom): rewrite for v0.11.0 scan-to-create`.

### Task 8.6: messaging/feishu.md

**Files:**
- Modify: `messaging/feishu.md`

- [ ] **Step 1:** Add **Document Comment Intelligent Reply** section: `drive.notice.comment_add_v1` event, 3-tier access control (exact/wildcard/top-level rules; `allowlist`/`pairing` policies), CLI tools (`python -m gateway.platforms.feishu_comment_rules ...`), required permissions.
- [ ] **Step 2:** Replace "ACK Emoji Reactions" with **Processing Status Reactions**: `Typing`/`CrossMark` lifecycle reactions, `FEISHU_REACTIONS=false` env var.
- [ ] **Step 3:** Add @mention preservation for agent consumption.
- [ ] **Step 4:** Commit `docs(feishu): document v0.11.0 comments and processing reactions`.

### Task 8.7: messaging/telegram.md

**Files:**
- Modify: `messaging/telegram.md`

- [ ] **Step 1:** Remove duplicate "## Webhook Mode" section (keep the longer/earlier one).
- [ ] **Step 2:** Add **"Sending Generated Files from Docker-backed Terminals"** section (MEDIA: host-path requirement with `terminal.docker_volumes`).
- [ ] **Step 3:** Add Topics-mode prerequisite caution.
- [ ] **Step 4:** Commit `docs(telegram): document Docker MEDIA + topics caution`.

### Task 8.8: messaging/slack.md

**Files:**
- Modify: `messaging/slack.md`

- [ ] **Step 1:** Replace stale callout claiming Slack lacks free-response (line ~286) — Slack now supports `SLACK_FREE_RESPONSE_CHANNELS` / `slack.free_response_channels`.
- [ ] **Step 2:** Add per-thread sessions for DMs by default + `slack.extra.dm_top_level_threads_as_sessions` knob.
- [ ] **Step 3:** Commit `docs(slack): document v0.11.0 free-response and DM thread sessions`.

### Task 8.9: messaging/qqbot.md

**Files:**
- Modify: `messaging/qqbot.md`

- [ ] **Step 1:** Fix command: `hermes setup gateway` → `hermes gateway setup`.
- [ ] **Step 2:** Rename env vars: `QQ_HOME_CHANNEL` → `QQBOT_HOME_CHANNEL`, `QQ_HOME_CHANNEL_NAME` → `QQBOT_HOME_CHANNEL_NAME`.
- [ ] **Step 3:** Remove `QQ_MARKDOWN_SUPPORT` env var (config-only in v0.11.0); add `QQ_SANDBOX` env var.
- [ ] **Step 4:** Add inline YAML comment: `markdown_support: true # config-only; no env-var equivalent`.
- [ ] **Step 5:** Commit `docs(qqbot): fix env-var names and command path`.

### Task 8.10: messaging/whatsapp.md

**Files:**
- Modify: `messaging/whatsapp.md`

- [ ] **Step 1:** Add `send_voice` native audio delivery (verify against `gateway/platforms/whatsapp.py` since canonical website doc may not yet include it).
- [ ] **Step 2:** Add `dm_policy` / `group_policy` parity with WeCom/Weixin/QQ (verify against source).
- [ ] **Step 3:** Commit `docs(whatsapp): document v0.11.0 voice and policy gating`.

### Task 8.11: messaging/weixin.md

**Files:**
- Modify: `messaging/weixin.md`

- [ ] **Step 1:** Note `qrcode` is bundled via `hermes-agent[messaging]` extra.
- [ ] **Step 2:** Replace "adapter rewrites Markdown to 【Title】..." with v0.11.0 Markdown-preservation behavior.
- [ ] **Step 3:** Add WeCom/Weixin QR-scan setup wizard reference.
- [ ] **Step 4:** Commit `docs(weixin): document Markdown preservation`.

### Task 8.12: messaging/signal.md

**Files:**
- Modify: `messaging/signal.md`

- [ ] **Step 1:** Replace "released through v0.8.0" framing.
- [ ] **Step 2:** Update install instructions (drop apt/snap, use brew or manual GitHub release; remove Docker `signal-cli-rest-api` mode).
- [ ] **Step 3:** Add **Media delivery via `send_message`**.
- [ ] **Step 4:** Commit `docs(signal): refresh install path and document send_message media`.

### Task 8.13: messaging/matrix.md

**Files:**
- Modify: `messaging/matrix.md`

- [ ] **Step 1:** Add `MATRIX_REACTIONS=true|false` env var.
- [ ] **Step 2:** Expand **"Deleting the crypto store"** recovery procedure (Synapse SQL/admin API steps, `/discardsession` note, regenerate-token easiest-path note).
- [ ] **Step 3:** Add PLATFORM_HINTS for Matrix.
- [ ] **Step 4:** Commit `docs(matrix): document v0.11.0 reactions and crypto recovery`.

### Task 8.14: messaging/webhooks.md

**Files:**
- Modify: `messaging/webhooks.md`

- [ ] **Step 1:** Add **Direct Delivery Mode** section: `deliver_only: true` flag, zero-LLM push notifications, examples (Supabase→Telegram, dynamic CLI subscription `--deliver-only`), HTTP response code table.
- [ ] **Step 2:** Add `deliver_only` field row to route config schema table.
- [ ] **Step 3:** Commit `docs(webhooks): document v0.11.0 direct-delivery mode`.

### Task 8.15: messaging/email.md and messaging/homeassistant.md (cosmetic)

**Files:**
- Modify: `messaging/email.md`
- Modify: `messaging/homeassistant.md`

- [ ] **Step 1:** Drop stale "released through v0.8.0" framing in both.
- [ ] **Step 2:** Reformat to canonical (no functional v0.11.0 deltas).
- [ ] **Step 3:** Commit `docs(messaging): refresh email and homeassistant framing`.

### Task 8.16: api-server.md

**Files:**
- Modify: `api-server.md`

- [ ] **Step 1:** Add **Inline image inputs** on `/v1/chat/completions` (`image_url` content parts) and `/v1/responses` (`input_image` parts). 400 error semantics for files and non-image data URLs.
- [ ] **Step 2:** Add `hermes.tool.progress` SSE event for Chat Completions.
- [ ] **Step 3:** Add spec-native `function_call` / `function_call_output` output items for Responses.
- [ ] **Step 4:** Note `previous_response_id` chains share session (single dashboard entry).
- [ ] **Step 5:** Add `GET /health/detailed` endpoint.
- [ ] **Step 6:** Add **Runs API**: `POST /v1/runs`, `GET /v1/runs/{run_id}/events` SSE.
- [ ] **Step 7:** Add **Jobs API**: full CRUD `/api/jobs/*` (list/create/get/patch/delete/pause/resume/run).
- [ ] **Step 8:** Remove "No file upload" limitation block.
- [ ] **Step 9:** Fix link path: `/features/profiles` → `/docs/user-guide/profiles`.
- [ ] **Step 10:** Commit `docs(api-server): document v0.11.0 API surface`.

---

## Phase 9 — Getting Started + Guides

### Task 9.1: getting-started/quickstart.md (major rewrite)

**Files:**
- Modify: `getting-started/quickstart.md`

- [ ] **Step 1:** Restructure to canonical: "Who this is for", "The fastest path" decision matrix, "How settings are stored", "Run Your First Chat" using `hermes --tui`, "Verify Sessions Work", "Add the Next Layer".
- [ ] **Step 2:** Replace inline provider table with the recommendation matrix; defer details to providers integration page.
- [ ] **Step 3:** Add NVIDIA NIM, Step Plan, Gemini CLI OAuth to provider mentions.
- [ ] **Step 4:** Add GPT-5.5 over Codex OAuth.
- [ ] **Step 5:** Add `hermes --continue` / `-c` resume verification step.
- [ ] **Step 6:** Add callout for `--ignore-user-config`, `--ignore-rules`.
- [ ] **Step 7:** Add Recovery Toolkit / Common Failure Modes if matching canonical.
- [ ] **Step 8:** Commit `docs(quickstart): rewrite for v0.11.0 with TUI and decision matrix`.

### Task 9.2: getting-started/installation.md and top-level installation.md (dedupe)

**Files:**
- Modify: `installation.md`
- Modify: `getting-started/installation.md`

- [ ] **Step 1:** Pick `getting-started/installation.md` as canonical.
- [ ] **Step 2:** Reduce top-level `installation.md` to a stub redirect.
- [ ] **Step 3:** Trim 200-line "Manual Installation" section to a one-paragraph link to `developer-guide/contributing.md#development-setup`.
- [ ] **Step 4:** Drop `git-lfs` prerequisite.
- [ ] **Step 5:** Add Docusaurus frontmatter (sidebar_position, title, description).
- [ ] **Step 6:** Commit `docs(installation): trim manual section, drop git-lfs, dedupe pages`.

### Task 9.3: getting-started/learning-path.md

**Files:**
- Modify: `getting-started/learning-path.md`

- [ ] **Step 1:** Add Docusaurus frontmatter.
- [ ] **Step 2:** Convert plain prose to `:::tip` admonitions where canonical does so.
- [ ] **Step 3:** Convert relative paths to `/docs/...` links.
- [ ] **Step 4:** Update Code Execution row to "Run Python scripts that call Hermes tools programmatically".
- [ ] **Step 5:** Add link to `use-voice-mode-with-hermes`.
- [ ] **Step 6:** Add orchestrator/delegation note in advanced track.
- [ ] **Step 7:** Commit `docs(learning-path): align with canonical and add v0.11.0 references`.

### Task 9.4: getting-started/termux.md

**Files:**
- Modify: `getting-started/termux.md`

- [ ] **Step 1:** Remove trailing "Platform Detection (v0.10.0)" section (canonical removes it).
- [ ] **Step 2:** Drop the v0.10.0-tagged ABI callout.
- [ ] **Step 3:** Commit `docs(termux): trim stale v0.10.0 callouts`.

### Task 9.5: getting-started/updating.md

**Files:**
- Modify: `getting-started/updating.md`

- [ ] **Step 1:** Add **"If your terminal disconnects mid-update"** subsection: SIGHUP protection, `~/.hermes/logs/update.log`, Ctrl-C/SIGTERM still honored, no longer needs `screen`/`tmux`.
- [ ] **Step 2:** Commit `docs(updating): document terminal-disconnect protection`.

### Task 9.6: guides/build-a-hermes-plugin.md

**Files:**
- Modify: `guides/build-a-hermes-plugin.md`

- [ ] **Step 1:** Update `post_tool_call` signature to include `duration_ms: int`.
- [ ] **Step 2:** Remove `pre_api_request` / `post_api_request` rows.
- [ ] **Step 3:** Add `on_session_finalize` and `on_session_reset` rows.
- [ ] **Step 4:** Commit `docs(plugin-guide): align hooks table with v0.11.0`.

### Task 9.7: guides/delegation-patterns.md

**Files:**
- Modify: `guides/delegation-patterns.md`

- [ ] **Step 1:** Add Docusaurus frontmatter.
- [ ] **Step 2:** Replace v0.10.0 constraints section with: "Default 3 parallel tasks (no hard ceiling)" + "Nested delegation is opt-in via orchestrator role and `delegation.max_spawn_depth > 1` (range 1-3); leaf subagents still cannot delegate; `delegation.orchestrator_enabled: false` disables".
- [ ] **Step 3:** Add **Tuning Concurrency and Depth** table: `max_concurrent_children` default 3, `max_spawn_depth` default 1 (range 1-3), with example YAML.
- [ ] **Step 4:** Commit `docs(delegation-patterns): document v0.11.0 orchestrator and depth tuning`.

### Task 9.8: guides/use-voice-mode-with-hermes.md

**Files:**
- Modify: `guides/use-voice-mode-with-hermes.md`

- [ ] **Step 1:** Add Docusaurus frontmatter.
- [ ] **Step 2:** Add `beep_enabled: true` to voice config block.
- [ ] **Step 3:** Commit `docs(voice-guide): add beep_enabled and frontmatter`.

### Task 9.9: guides/work-with-skills.md

**Files:**
- Modify: `guides/work-with-skills.md`

- [ ] **Step 1:** Add direct HTTP(S) `SKILL.md` install examples: `hermes skills install https://sharethis.chat/SKILL.md` and `/skills install https://example.com/SKILL.md --name my-skill`.
- [ ] **Step 2:** Commit `docs(skills-guide): document direct HTTPS install`.

### Task 9.10: minor frontmatter sweeps (guides)

**Files:**
- Modify: `guides/automate-with-cron.md`
- Modify: `guides/local-llm-on-mac.md`
- Modify: `guides/migrate-from-openclaw.md`

- [ ] **Step 1:** Add Docusaurus frontmatter to each (only diff vs canonical).
- [ ] **Step 2:** Commit `docs(guides): add Docusaurus frontmatter`.

---

## Phase 10 — Final review, integration, push

### Task 10.1: cross-link sweep

**Files:** all touched docs

- [ ] **Step 1:** Run a grep to verify no remaining `claude-sonnet-4-6`, `claude-opus-4.6`, `gpt-5.4`, `flush_memories`, `api_key_env` in docs (allow exceptions only when explicitly documenting deprecated names).

```bash
cd ~/src/hermes/hermes-agent-docs
git grep -E 'claude-sonnet-4-6|claude-opus-4\.6|gpt-5\.4|flush_memories|api_key_env' -- '*.md' | grep -v -E '(deprecated|removed)' || echo "clean"
```

- [ ] **Step 2:** Run a grep for stale version markers:

```bash
git grep -E 'released.*v0\.(8|9|10)\.0|through v0\.10\.0|Current.*v0\.10\.0' -- '*.md' | grep -v changelog.md || echo "clean"
```

- [ ] **Step 3:** Verify `/steer` is mentioned in `slash-commands.md`, `gateway.md`, `messaging/index.md`, `messaging/README.md`, `tools.md` (cross-linked).
- [ ] **Step 4:** Verify `transport-layer.md` is linked from `architecture.md`, `developer-guide/architecture.md`, `developer-guide/provider-runtime.md`, `developer-guide/agent-loop.md`, `developer-guide/adding-providers.md`.
- [ ] **Step 5:** If issues found, fix in a single sweep commit `docs: cross-link and consistency sweep`.

### Task 10.2: build/lint check

**Files:** none

- [ ] **Step 1:** If a docs build/lint command exists (Docusaurus build, vale, markdownlint), run it.

```bash
cd ~/src/hermes/hermes-agent-docs
ls -la package.json 2>/dev/null && cat package.json | jq -r '.scripts | keys[]' 2>/dev/null || echo "no package.json"
```

- [ ] **Step 2:** If a build script exists, run it; otherwise skip.
- [ ] **Step 3:** Fix any link/build errors and commit `docs: fix build warnings`.

### Task 10.3: push

**Files:** none

- [ ] **Step 1:** Confirm clean diff:

```bash
git status
git log origin/main..HEAD --oneline
```

- [ ] **Step 2:** Push:

```bash
git push origin main
```

- [ ] **Step 3:** Confirm push succeeded with `git status` showing "Your branch is up to date with 'origin/main'".

---

## Self-review

**Spec coverage:**
- Changelog v0.11.0: Task 1.1 ✓
- Transport ABC: Tasks 2.2, 2.3, 4.1, 4.2, 4.3, 4.4 ✓
- Native AWS Bedrock: Tasks 3.1, 4.2 ✓
- Five new providers (NIM, Arcee, Step Plan, Gemini CLI OAuth, ai-gateway): Tasks 3.1, 3.4, 4.2 ✓
- Ink-based TUI: Tasks 2.1, 9.1 (cli-commands.md adds `--tui`) ✓
- QQBot 17th platform: Tasks 2.1, 6.2, 7.2, 8.1, 8.2, 8.9 ✓
- Plugin surface expansion: Tasks 2.5, 2.6, 4.6, 9.6 ✓
- /steer: Tasks 3.5, 4.3, 5.3, 6.1, 8.2 ✓
- Webhook direct-delivery: Tasks 6.1, 8.14 ✓
- Orchestrator + max_spawn_depth: Tasks 2.4, 3.2, 4.3, 6.1, 9.7 ✓
- Honcho overhaul + path correction: Task 5.2 ✓
- Dashboard plugin system + theme switching: Tasks 5.5, 5.8 ✓
- Auxiliary models UI: Tasks 3.2, 5.6 ✓
- Image-gen pluggable backends: Tasks 2.7, 6.1, 6.2, 7.1, 7.2 ✓
- New TTS/STT (Gemini, KittenTTS, xAI): Tasks 6.6, 6.7 ✓
- Cron wakeAgent + enabled_toolsets: Task 6.8 ✓
- New skills + xitter→xurl: Tasks 6.3, 7.3 ✓
- API server inline images + Runs/Jobs: Task 8.16 ✓
- Discord forum channels + role-based access: Task 8.3 ✓
- Feishu document comments + processing reactions: Task 8.6 ✓
- WeCom/Weixin scan-to-create: Tasks 8.5, 8.11 ✓
- DingTalk QR device-flow + AI Cards: Task 8.4 ✓
- Slack per-thread DMs + free-response: Task 8.8 ✓
- Browser cdp + Camofox: Task 6.4 ✓
- Code-execution project/strict modes: Task 6.5 ✓
- Tool-output truncation limits, code execution mode, browser dialog policy: Task 3.2 ✓
- Doctor command-installation check, hermes skills reset, --ignore-user-config / --ignore-rules: Tasks 3.3, 6.3 ✓
- Memory/sessions: Tasks 5.1, 5.3 ✓
- Updating SIGHUP protection: Task 9.5 ✓
- FAQ model strings + shell init files: Task 7.5 ✓

**Placeholder scan:** No "TBD" / "fill in details" / "similar to Task N" placeholders. Every step names a concrete artifact or text change.

**Type consistency:** `delegation.max_spawn_depth` (range 1-3, default 1), `delegation.max_concurrent_children` (default 3), `delegation.orchestrator_enabled` (boolean) — used consistently across Tasks 2.4, 3.2, 4.3, 6.1, 9.7. `voice.beep_enabled` consistent across 6.6, 6.7, 9.8. `code_execution.mode: project | strict` consistent across 3.2 and 6.5.

**Authoritative sources for executors:**
- `~/src/hermes/hermes-agent/RELEASE_v0.11.0.md`
- `~/src/hermes/hermes-agent/website/docs/` (canonical mirror)
- Five validation reports at `/private/tmp/claude-501/-Users-mudrii-src-hermes/94926239-2e1e-42ed-aa8d-166e247fe738/tasks/{aaad54ffbee20a803,a5dc98dbd13395fcb,a4ca5b30264a8d349,aca7d1f0cd3bfe73d,ad832346bde2fd1d6}.output`
